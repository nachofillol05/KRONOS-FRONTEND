import React from 'react';
import './fondos.scss';
import Modal from '../modal/modals.jsx';
export default function Fondo({ children}) {

    return (
        <section>
            {children}
            
        </section>
    );
}
